# 지능형 노드 기반 챗봇 빌더 시스템 명세서

**버전:** 1.0  
**작성일:** 2025년 2월  
**문서 상태:** 확정

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [실행 모델](#2-실행-모델)
3. [노드 상세 명세](#3-노드-상세-명세)
4. [데이터 아키텍처](#4-데이터-아키텍처)
5. [변수 및 스코프 체계](#5-변수-및-스코프-체계)
6. [버전 관리 및 배포](#6-버전-관리-및-배포)
7. [로깅 및 모니터링](#7-로깅-및-모니터링)
8. [보안](#8-보안)
9. [확장성](#9-확장성)

---

## 1. 프로젝트 개요

### 1.1 프로젝트 목표

- **비선형 그래프 빌더:** 복잡한 분기와 루프를 지원하는 GUI 환경 제공
- **데이터 중심 실행 엔진:** 변수와 상태 기반의 동적 워크플로우 제어
- **인간-AI 협업(HITL):** 고부하 업무의 안전성을 보장하는 하이브리드 운영 모델

### 1.2 서비스 구조

```
테넌트 (회사/조직)
  └── 프로젝트
        ├── 문서셋 (독립 버전 관리)
        │     └── 문서들 (n:n 관계, 폴더 구조)
        └── 봇 (독립 버전 관리)
              └── 플로우 (노드 그래프)
```

### 1.3 지원 채널

- 채팅 (웹챗, 카카오톡 등)
- 이메일
- API 호출

챗봇 생성 시 채널을 선택하며, 선택된 채널에서 지원하는 메시지 타입만 저작 도구에 노출됩니다.

---

## 2. 실행 모델

### 2.1 핵심 개념

#### 2.1.1 노드 = 상태 (State Machine 모델)

시스템은 상태 머신 기반으로 동작합니다. 각 노드는 상태(State)를 의미하며, 세션은 항상 하나의 "현재 노드"를 가집니다.

#### 2.1.2 전이 특성

노드는 유형에 따라 두 가지 전이 특성을 가집니다:

| 전이 특성 | 설명 | 동작 |
|----------|------|------|
| **Blocking** | 외부 트리거 대기 | 사용자 입력, 상담원 승인 등 대기 |
| **Non-blocking** | 즉시 전이 | 처리 완료 후 다음 노드로 자동 전이 |

#### 2.1.3 턴(Turn) 정의

```
[외부 트리거] → [Non-blocking 노드 순차 실행] → [Blocking 노드 도달]
└─────────────────────── 1턴 ───────────────────────────┘
```

- Blocking 노드에 도달하면 턴 종료
- 다음 트리거(사용자 입력, 상담원 승인 등)를 대기

### 2.2 노드 유형별 전이 특성

| 노드 유형 | 전이 특성 | 전이 조건 |
|----------|----------|----------|
| 메시지 출력 노드 | 옵션 (기본 Blocking) | "응답 대기" 옵션에 따름 |
| 의도 분류 노드 | Non-blocking | 분류 완료 즉시 |
| RAG 검색 노드 | Non-blocking | 답변 생성 완료 즉시 |
| 조건 분기 노드 | Non-blocking | 조건 평가 즉시 |
| Parallel 노드 | Non-blocking | 분기 즉시 |
| Join 노드 | Non-blocking | 모든 경로 완료 시 |
| API 커넥터 노드 | Non-blocking | API 응답 수신 즉시 |
| 커스텀 코드 노드 | Non-blocking | 스크립트 실행 완료 즉시 |
| HITL 에스컬레이션 노드 | Blocking | 상담원 개입 완료 시 (세션 종료) |
| HITL 승인 노드 | Blocking | 상담원 승인/거절 시 |
| 시작 노드 | - | 트리거 발생 시 |
| 종료 노드 | - | 세션 종료 |
| 에러 폴백 노드 | - | 에러 발생 시 |

### 2.3 분기 vs 병렬

#### 2.3.1 분기 (Conditional Branch)

조건에 따라 **하나의 경로만** 실행됩니다.

```
[조건 분기] ─┬→ [경로 A]
             ├→ [경로 B]  ← 하나만 실행
             └→ [경로 C]
```

- 출력 핸들러 개수 제한 없음
- 별도 Merge 노드 불필요

#### 2.3.2 병렬 (Parallel + Join)

여러 경로가 **동시에** 실행되고 합쳐집니다.

```
[Parallel (3)] ─┬→ [작업 A] ──┬→ [Join (3)]
                ├→ [작업 B] ──┤
                └→ [작업 C] ──┘
```

- Parallel 노드에서 핸들러 개수 명시 (예: 3개)
- Join 노드에서도 동일한 핸들러 개수 명시
- **Parallel~Join 구간은 Non-blocking 노드만 허용**
- 저작자가 명시적으로 "몇 개로 나누고 합칠지" 인지 가능

### 2.4 연쇄 실행 및 루프 방지

#### 2.4.1 연쇄 실행

Non-blocking 노드들은 순차적으로 실행됩니다 (동시 실행 아님).

```
[의도 분류] → [조건 분기] → [API 호출] → [메시지]
   1번째        2번째         3번째        4번째 (순차 실행)
```

#### 2.4.2 루프 방지 정책

시스템 레벨 안전장치를 적용합니다:

| 제한 | 기본값 |
|------|--------|
| 한 턴 내 동일 노드 최대 실행 횟수 | 5회 |
| 한 턴 내 총 노드 실행 횟수 | 50회 |

- 초과 시 자동으로 에러 폴백 트리거
- 고급 옵션: 노드별 "최대 재진입 횟수" 오버라이드 가능 (숨김 옵션)

### 2.5 에러 핸들링

#### 2.5.1 에러 폴백 구조

- **노드별 에러 폴백:** 특정 노드에 개별 에러 폴백 지정 가능
- **전역 에러 폴백:** 노드별 미지정 시 적용되는 기본 에러 폴백

#### 2.5.2 체크포인트 시스템

에러 발생 시:

1. 마지막 성공 노드까지 체크포인트 기록
2. 세션 상태에 오류 정보 저장
3. 에러 폴백 노드로 분기

#### 2.5.3 재시작 옵션

에러 폴백 노드에서 저작자가 선택 가능:

- "마지막 성공 노드부터 재실행"
- "실패한 노드부터 재실행"

### 2.6 세션 생명주기

#### 2.6.1 세션 상태 다이어그램

```
[INIT] → [ACTIVE] → [WAITING_INPUT] → [ACTIVE] → ...
              ↓            ↓
         [WAITING_HUMAN]   ↓
              ↓            ↓
              → → → → [TIMEOUT] → [CLOSED]
                           ↓
                      [COMPLETED] → [CLOSED]
```

#### 2.6.2 세션 시작

저작자가 시작 트리거 선택 가능:

- 사용자 첫 메시지
- 채팅창 오픈
- API 호출
- 이메일 수신

#### 2.6.3 세션 타임아웃

- 타임아웃 전용 트리거로 처리
- 저작자가 타임아웃 후 동작 설계:
  - 세션 종료 플로우 태우기
  - 세션 유지하고 재진입 허용
  - 기타 커스텀 처리

#### 2.6.4 Blocking 노드 타임아웃

- Blocking 노드에 타임아웃 핸들러 옵션 추가
- 핸들러 있으면 → 지정된 경로로 분기
- 핸들러 없으면 → 에러 처리 → 에러 폴백으로

#### 2.6.5 다중 세션 정책

- **단일 세션 강제** (기본)
- 동일 사용자는 하나의 활성 세션만 유지
- 다른 디바이스/채널 접속 시 기존 세션 이어서 진행

---

## 3. 노드 상세 명세

### 3.1 메시지 출력 노드

#### 3.1.1 기본 속성

| 속성 | 설명 | 기본값 |
|------|------|--------|
| 메시지 목록 | 한 턴에 출력할 메시지들 (다중 입력 가능) | - |
| 사용자 응답 대기 | Blocking 여부 | ON |
| 타임아웃 | 응답 대기 시간 (대기 ON일 때) | 설정값 |
| 타임아웃 핸들러 | 타임아웃 시 분기 경로 | 없음 (에러 폴백) |

#### 3.1.2 지원 메시지 타입

채널별로 지원하는 타입이 다르며, 챗봇 생성 시 선택한 채널에 따라 노출됩니다.

| 타입 | 설명 |
|------|------|
| 텍스트 | 일반 텍스트 메시지 |
| 이미지 | 이미지 첨부 |
| 버튼 | 빠른 응답 버튼 |
| 카드/캐러셀 | 상품 목록, 옵션 선택 등 |
| 링크 | 외부 페이지 연결 |

#### 3.1.3 버튼 동작 방식

각 버튼 설정:

| 속성 | 설명 |
|------|------|
| 표시 텍스트 | 사용자에게 보이는 버튼 레이블 |
| payload | 선택 시 저장할 값 (선택사항) |

동작:

- **payload 미입력:** 표시 텍스트가 사용자 입력으로 전달
- **payload 입력:** payload가 지정 변수에 저장

이를 통해 자유 입력과 버튼 입력의 혼합 대응이 가능합니다.

#### 3.1.4 다중 메시지 지원

하나의 메시지 노드에서 여러 메시지를 순차 출력할 수 있습니다:

```
메시지 노드:
  - 메시지 1: "예약이 완료되었습니다! 🎉"
  - 메시지 2: "예약 번호는 {{session.reservationId}}입니다."
  - 메시지 3: "다른 문의사항이 있으신가요?"
  사용자 응답 대기: ON
```

### 3.2 RAG 검색 노드

#### 3.2.1 기본 속성

| 속성 | 설명 |
|------|------|
| 문서셋 (폴더) | 검색 대상 문서 폴더 선택 |
| 버전 | "최신" 또는 특정 버전 선택 |
| Top-K | 검색 결과 개수 |
| 유사도 임계값 | 최소 유사도 점수 |
| 페르소나 설정 | LLM 응답 스타일 |

#### 3.2.2 문서셋 관리

- 문서는 중앙에서 관리
- 문서와 폴더는 **n:n 관계** (하나의 문서가 여러 폴더에 속할 수 있음)
- RAG 노드에서 대상 폴더(문서셋) 선택

#### 3.2.3 버전 참조 방식

| 방식 | 설명 | 용도 |
|------|------|------|
| 최신 참조 | 폴더의 최신 버전 자동 사용 | 문서 자유 업데이트 |
| 버전 참조 | 특정 버전 고정 | A/B 테스트 |

#### 3.2.4 출력 변수

| 변수 | 설명 |
|------|------|
| `{{nodes.[노드ID].answer}}` | 생성된 답변 |
| `{{nodes.[노드ID].confidence}}` | 신뢰도 점수 (벡터 유사도 기반) |
| `{{nodes.[노드ID].sources}}` | 참조 문서 목록 |

#### 3.2.5 실패 처리

- 검색 실패/결과 없음도 LLM에게 전달
- LLM이 컨텍스트에 맞게 응답 생성 ("정보를 찾지 못했습니다" 등)
- 별도 분기 경로 불필요

### 3.3 의도 분류 노드

#### 3.3.1 기본 속성

| 속성 | 설명 |
|------|------|
| 의도 목록 | 분류할 의도들 |
| 예시 발화 (선택) | 의도별 예시 문장 (Few-shot) |
| 설명 (선택) | 의도별 상세 설명 |

#### 3.3.2 의도 정의 방식

저작자가 원하는 정확도 수준에 따라 선택:

| 방식 | 정확도 | 작업량 |
|------|--------|--------|
| 의도 목록만 | 기본 | 낮음 |
| + 예시 발화 | 향상 | 중간 |
| + 설명 | 최상 | 높음 |

#### 3.3.3 분기 역할

의도 분류 노드는 **분기 노드 역할을 겸합니다**:

```
[의도 분류 노드]
  의도 목록:
    - 예약 → (핸들러) → [예약 플로우]
    - 취소 → (핸들러) → [취소 플로우]
    - 기타 → (핸들러) → [RAG 노드]
```

별도 조건 분기 노드 불필요.

### 3.4 조건 분기 노드

#### 3.4.1 기본 속성

| 속성 | 설명 |
|------|------|
| 조건 목록 | 변수 기반 조건식들 |
| 기본 경로 | 모든 조건 불일치 시 |

#### 3.4.2 조건 형식

```
{{session.userGrade}} == "VIP"
{{session.amount}} >= 100000
{{nodes.apiCall.result.status}} == "success"
```

### 3.5 Parallel / Join 노드

#### 3.5.1 Parallel 노드

| 속성 | 설명 |
|------|------|
| 핸들러 개수 | 병렬 분기 수 (명시적 지정) |

#### 3.5.2 Join 노드

| 속성 | 설명 |
|------|------|
| 핸들러 개수 | Parallel과 동일하게 지정 |

#### 3.5.3 제약사항

- Parallel~Join 구간 내에는 **Blocking 노드 금지**
- Non-blocking 노드만 허용
- 모든 경로 완료 시 Join 통과 (AND 조건)

### 3.6 API 커넥터 노드

#### 3.6.1 기본 속성

| 속성 | 설명 |
|------|------|
| URL | API 엔드포인트 (변수 치환 가능) |
| Method | GET, POST, PUT, DELETE 등 |
| Headers | 요청 헤더 |
| Request Body | 요청 본문 (변수 치환 지원) |
| Retry Policy | 재시도 정책 |

#### 3.6.2 인증 처리

인증 방식 선택 UI 제공:

| 방식 | 설명 |
|------|------|
| API Key | Header에 키 삽입 |
| Bearer Token | Authorization 헤더 |
| OAuth 2.0 | 토큰 자동 갱신 지원 |
| Basic Auth | 사용자명/비밀번호 |

- 자격증명은 별도 관리 (암호화 저장)

#### 3.6.3 응답 매핑

- 전체 응답은 자동 저장: `{{nodes.[노드ID].response}}`
- 저작자가 필요 시 JSONPath로 특정 값 추출 가능
- 저작 화면에서 테스트 실행 → 실제 응답 확인 가능

### 3.7 커스텀 코드 노드

#### 3.7.1 실행 환경

- 샌드박스화된 JavaScript 환경 (V8 Isolate)
- 프론트엔드에서 실행 가능한 수준

#### 3.7.2 제약 조건

| 항목 | 값 |
|------|-----|
| 실행 시간 제한 | 10초 |
| 메모리 제한 | 64MB |
| 네트워크 접근 | 금지 |
| 파일 시스템 접근 | 금지 |

#### 3.7.3 접근 권한

| 스코프 | 읽기 | 쓰기 |
|--------|------|------|
| System | ✅ | ❌ |
| Session | ✅ | ✅ |
| Nodes | ✅ | ❌ |

#### 3.7.4 허용 라이브러리

- Lodash (데이터 가공)
- Day.js (날짜 처리)
- UUID (고유 ID 생성)

#### 3.7.5 사용 예시

```javascript
// 접근 가능
input.system.apiUrl       // 읽기만
input.session.userName    // 읽기/쓰기
input.nodes.apiCall.result // 읽기만

// 결과 저장
setVariable('session.finalPrice', 15000);
return { success: true };  // nodes.[노드ID].result로도 접근 가능
```

### 3.8 HITL 에스컬레이션 노드

#### 3.8.1 기능

상담원에게 대화 제어권을 완전히 이전합니다.

#### 3.8.2 속성

| 속성 | 설명 |
|------|------|
| 전달 정보 | AI 요약 (자동 생성) |

#### 3.8.3 동작

1. 상담원에게 요약 정보 전달
2. 제어권 완전 이전
3. 상담원이 직접 대화 진행
4. **상담원 종료 시 세션 종료** (플로우 복귀 없음)

### 3.9 HITL 승인 노드

#### 3.9.1 기능

AI가 작업을 준비하고, 상담원 승인 후 실행하는 비동기 게이트입니다.

#### 3.9.2 속성

| 속성 | 설명 | 기본값 |
|------|------|--------|
| 승인 요청 정보 | 상담원에게 보여줄 내용 | AI 요약 (자동) |
| 추가 메시지 | 커스텀 정보 추가 | - |
| 대기 메시지 | 사용자에게 표시할 메시지 | "처리 중입니다" |
| 타임아웃 | 승인 대기 시간 | 없음 (무기한) |

#### 3.9.3 동작 흐름

```
[사용자]: "환불해주세요"
    ↓
[AI 처리]: 환불 정보 준비
    ↓
[승인 노드 진입]
    ↓
[고객 화면]: "처리 중입니다. 잠시만 기다려주세요" (대기)
    ↓
[상담원 대시보드]: 승인 요청 알림 → [승인] 또는 [거절] 클릭
    ↓
[승인/거절] → 플로우 재개
```

#### 3.9.4 분기 경로

- **승인 핸들러:** 승인 시 진행할 경로
- **거절 핸들러:** 거절 시 진행할 경로

#### 3.9.5 거절 사유

- 상담원이 거절 시 사유 입력
- `{{nodes.[노드ID].rejectReason}}`으로 접근 가능

### 3.10 시작 노드

#### 3.10.1 기능

모든 플로우의 진입점입니다.

#### 3.10.2 속성

| 속성 | 설명 |
|------|------|
| 트리거 유형 | 채팅 / 이메일 / API |

채널에 따라 초기 컨텍스트가 다르게 설정됩니다.

### 3.11 종료 노드

#### 3.11.1 기능

세션을 정상 종료합니다.

#### 3.11.2 동작

- 세션 데이터 보관 (기본)
- 종료 메시지는 별도 메시지 노드로 처리
- 만족도 조사는 종료 전에 노드로 끼워넣기 가능

### 3.12 에러 폴백 노드

#### 3.12.1 기능

에러 발생 시 처리하는 노드입니다.

#### 3.12.2 설정 범위

- **노드별:** 특정 노드에 개별 에러 폴백 지정
- **전역:** 노드별 미지정 시 적용되는 기본 에러 폴백

#### 3.12.3 재시작 옵션

저작자가 선택:

- "마지막 성공 노드부터 재실행"
- "실패한 노드부터 재실행"

---

## 4. 데이터 아키텍처

### 4.1 기술 스택

| 용도 | 기술 | 비고 |
|------|------|------|
| RDB | PostgreSQL | JSONB 활용 |
| 캐시/세션 | Redis | 세션 상태 관리 |
| 벡터 DB | pgvector | PostgreSQL 확장 |
| 파일 저장소 | 로컬 파일시스템 | 추후 S3 전환 |

### 4.2 저장소별 데이터

| 데이터 유형 | 저장소 |
|------------|--------|
| 플로우 정의 | PostgreSQL (JSONB) |
| 문서셋 원본 | 로컬 파일시스템 (추후 S3) |
| 벡터 임베딩 | pgvector |
| 세션 상태 | Redis |
| 대화 로그 | PostgreSQL |
| 통계/분석 | PostgreSQL |

### 4.3 세션 영속성

#### 4.3.1 MVP 설계

```
session:{sessionId} = {
  status: "ACTIVE" | "WAITING_INPUT" | "WAITING_HUMAN" | ...,
  currentNodeId: "node_abc123",
  variables: { ... },
  checkpoint: { nodeId, timestamp, snapshot },
  createdAt: timestamp,
  updatedAt: timestamp
}
TTL: 24시간 (마지막 활동 기준)
```

**동기화 정책:**

- 세션 진행 중: Redis만 사용
- 세션 종료 시: PostgreSQL에 최종 상태 + 대화 로그 저장, Redis 삭제
- Redis 장애 시: 진행 중 세션 유실 감수

#### 4.3.2 실서비스 전환 대비

MVP 단계에서 준비할 것:

1. **세션 저장소 추상화 계층**

```typescript
interface SessionStore {
  get(sessionId): Session
  set(sessionId, session): void
  delete(sessionId): void
}

// MVP: RedisSessionStore
// 실서비스: WriteThruSessionStore (Redis + PostgreSQL)
```

2. **체크포인트 구조** MVP부터 설계
3. **종료 시 PostgreSQL 저장** MVP부터 구현

### 4.4 대화 로그 스키마

#### 4.4.1 세션 테이블

```sql
CREATE TABLE sessions (
  id UUID PRIMARY KEY,
  tenant_id UUID NOT NULL,
  bot_id UUID NOT NULL,
  channel VARCHAR(20),           -- chat / email / api
  status VARCHAR(20),            -- completed / timeout / escalated / error
  started_at TIMESTAMP,
  ended_at TIMESTAMP,
  summary TEXT,                  -- AI 요약
  variables_snapshot JSONB       -- 종료 시점 세션 변수
);
```

#### 4.4.2 메시지 테이블

```sql
CREATE TABLE messages (
  id UUID PRIMARY KEY,
  session_id UUID REFERENCES sessions(id),
  direction VARCHAR(10),         -- inbound / outbound
  content TEXT,
  content_type VARCHAR(20),      -- text / image / button / carousel
  created_at TIMESTAMP
);
```

#### 4.4.3 노드 실행 이력 테이블

```sql
CREATE TABLE node_executions (
  id UUID PRIMARY KEY,
  session_id UUID REFERENCES sessions(id),
  node_id VARCHAR(100),
  node_type VARCHAR(50),
  input_snapshot JSONB,          -- 노드 진입 시 변수 상태
  output_snapshot JSONB,         -- 노드 완료 시 결과
  status VARCHAR(20),            -- success / error
  error_detail TEXT,
  started_at TIMESTAMP,
  ended_at TIMESTAMP
);
```

#### 4.4.4 민감정보 처리

- 노드별 "민감정보 포함" 옵션 설정
- 해당 옵션 활성화 시 **저장 전 마스킹 처리**

---

## 5. 변수 및 스코프 체계

### 5.1 스코프 정의

| 스코프 | 설명 | 예시 |
|--------|------|------|
| **System** | 전역 설정 (읽기 전용) | API URL, 봇 설정 |
| **Session** | 현재 대화 데이터 | 사용자 정보, 예약일시, 인증여부 |
| **Nodes** | 노드 실행 결과 (읽기 전용) | 이전 노드 출력값 |

### 5.2 노드 결과 저장 구조

#### 5.2.1 내부 저장 구조

```json
{
  "session": {
    "userName": "홍길동",
    "reservationId": "ABC123",
    
    "_nodes": {
      "intentClassifier_1": {
        "intent": "reservation",
        "confidence": 0.92,
        "_executedAt": "2024-01-15T10:30:00Z"
      },
      "apiCall_1": {
        "result": { "userId": 123, "userName": "홍길동" },
        "statusCode": 200,
        "_executedAt": "2024-01-15T10:30:01Z"
      }
    }
  }
}
```

#### 5.2.2 접근 방식

두 가지 방식 모두 지원:

```
{{session.userName}}                      // 명시적 저장 변수
{{nodes.apiCall_1.result.userName}}       // 노드 직접 참조
```

### 5.3 노드 ID 체계

| 구분 | 설명 |
|------|------|
| 내부 ID | 자동 생성 (UUID 또는 타입+순번) |
| 별칭 | 저작자가 선택적으로 지정 가능 |

접근 시 별칭 또는 내부 ID 둘 다 사용 가능:

```
{{nodes.예약조회API.result}}       // 별칭 사용
{{nodes.node_a1b2c3d4.result}}    // 내부 ID 사용
```

### 5.4 노드 설정 시 변수 저장

각 노드에서 결과를 세션 변수에 저장할 수 있습니다:

```
[API 호출 노드]
├── ... 기타 설정 ...
└── 결과 저장 (선택):
    └── session.userData ← 저작자가 원하면 지정
```

### 5.5 Variable Picker

노드 설정 시 `{{` 입력으로 사용 가능한 변수 자동 완성 지원.

---

## 6. 버전 관리 및 배포

### 6.1 버전 관리 단위

| 리소스 | 버전 관리 |
|--------|----------|
| 플로우 | 독립 버전 |
| 문서셋 | 독립 버전 |

각각 별도로 배포/롤백 가능합니다.

### 6.2 배포 정책

| 세션 상태 | 적용 버전 |
|----------|----------|
| 신규 세션 | 새 버전 적용 |
| 진행 중 세션 | 기존 버전 유지 |

### 6.3 플로우-문서셋 버전 조합

RAG 노드에서 문서셋 버전 참조 방식 선택:

| 방식 | 설명 | 용도 |
|------|------|------|
| 최신 참조 | 폴더의 최신 버전 자동 사용 | 문서 자유 업데이트 |
| 버전 참조 | 특정 버전 고정 | A/B 테스트 |

```
[RAG 노드 설정]
문서셋: [FAQ 폴더]
버전: [최신 ▼] 또는 [v3 ▼]
```

### 6.4 테스트 및 시뮬레이션

#### 6.4.1 테스트 모드

- **실제 API 호출** ✅
- **실제 LLM 호출** ✅
- 세션 변수는 테스트 창에서 입력한 값 사용
- 테스트 전용 환경 (운영 데이터 영향 없음)

#### 6.4.2 테스트 데이터

- 테스트 창에서 세션 변수 필드 표시
- 저작자가 직접 입력

#### 6.4.3 테스트 이력

- 저장하지 않음

---

## 7. 로깅 및 모니터링

### 7.1 로그 범위

모든 데이터를 유기적으로 연결하여 추적 가능:

| 항목 | 저장 |
|------|------|
| 사용자 입력 원문 | ✅ |
| 봇 응답 원문 | ✅ |
| 노드 실행 이력 | ✅ |
| 변수 변경 이력 | ✅ |
| LLM 프롬프트/응답 | ✅ |
| API 요청/응답 | ✅ |
| 에러 상세 | ✅ |

### 7.2 민감정보 처리

- 노드별 "민감정보 포함" 옵션
- 해당 옵션 활성화 시 로그에 마스킹 처리

### 7.3 실시간 대시보드

| 지표 | 설명 |
|------|------|
| 현재 활성 세션 수 | 실시간 세션 카운트 |
| 에러 발생률 | 에러 비율 |
| HITL 대기 건수 | 상담원 대기 중인 건 |

### 7.4 통계 및 분석

#### 7.4.1 기본 통계

| 항목 | 설명 |
|------|------|
| 일별/주별 세션 수 | 트래픽 추이 |
| 평균 대화 턴 수 | 대화 깊이 |
| 세션 완료율 | 종료 노드 도달 비율 |
| 이탈 지점 분석 | 어느 노드에서 이탈하는지 |
| 의도별 분포 | 의도 분류 결과 통계 |
| HITL 전환율 | 상담원 개입 비율 |
| 평균 응답 시간 | 봇 응답 속도 |

#### 7.4.2 고급 분석

- A/B 테스트 결과 비교
- 만족도 점수 추이
- 노드별 성능 (실행 시간, 실패율)

#### 7.4.3 리포트

- 대시보드 실시간 조회
- 데이터 내보내기 (CSV)

### 7.5 감정 분석

- **최종 방향:** 시스템 레벨 자동 처리
- 모든 사용자 입력에 백그라운드 감정 태깅
- 통계/대시보드에서 활용
- 플로우 노드로는 노출하지 않음
- **MVP에서는 스킵**, 이후 버전에서 구현

---

## 8. 보안

### 8.1 권한 관리

#### 8.1.1 계층 구조

```
테넌트 (회사/조직)
  └── 프로젝트
        ├── 문서셋 ← 개별 권한
        └── 봇 ← 개별 권한
```

#### 8.1.2 권한 항목

| 리소스 | 권한 | 설명 |
|--------|------|------|
| 프로젝트 | 관리 | 프로젝트 설정, 멤버 초대 |
| 프로젝트 | 조회 | 프로젝트 내 리소스 목록 보기 |
| 봇 | 편집 | 플로우 저작, 설정 변경 |
| 봇 | 배포 | 테스트 → 운영 배포 |
| 봇 | 상담 | HITL 대응, 실시간 모니터링 |
| 봇 | 조회 | 통계, 로그 조회 |
| 문서셋 | 편집 | 문서 업로드, 수정, 삭제 |
| 문서셋 | 조회 | 문서 내용 보기 |

#### 8.1.3 역할 프리셋

| 역할 | 프로젝트 | 봇 | 문서셋 |
|------|---------|-----|--------|
| 프로젝트 관리자 | 관리, 조회 | 전체 | 전체 |
| 빌더 | 조회 | 편집, 배포, 조회 | 편집, 조회 |
| 상담원 | 조회 | 상담, 조회 | - |
| 뷰어 | 조회 | 조회 | 조회 |

#### 8.1.4 권한 부여 방식

- **역할 프리셋** 기본 적용
- **개별 오버라이드** 가능

```
홍길동:
  - 프로젝트 A: 빌더 (역할 프리셋)
  - 프로젝트 A > 봇1: 배포 권한 제외 (커스텀 오버라이드)
  - 프로젝트 A > 문서셋2: 접근 불가
```

#### 8.1.5 구현 방식

- **PostgreSQL RLS (Row Level Security)** 활용
- 테넌트/프로젝트/봇/문서셋 레벨 정책 적용
- DB 레벨에서 권한 강제, 앱 로직 누락 방지

### 8.2 데이터 암호화

#### 8.2.1 저장 데이터 (At Rest)

| 데이터 | 암호화 |
|--------|--------|
| 플로우 정의 | ❌ |
| 문서셋 원본 | ❌ |
| 대화 로그 | ✅ |
| 세션 변수 | ✅ |
| API 자격증명 | ✅ |

- 방식: 앱 레벨 일반 암호화

#### 8.2.2 전송 데이터 (In Transit)

| 구간 | 암호화 |
|------|--------|
| 인바운드/아웃바운드 | HTTPS 기본 |
| 내부 통신 | 불필요 |

### 8.3 멀티 테넌트 격리

- **Row 레벨 격리 + RLS**
- 모든 테이블에 tenant_id 컬럼
- PostgreSQL RLS로 테넌트 간 데이터 격리

---

## 9. 확장성

### 9.1 MVP 목표

| 항목 | 목표 |
|------|------|
| TPS | 50 |
| 동시 활성 세션 | 수백 개 |
| 일일 대화 처리량 | 수만 건 |

### 9.2 서버 구조

- **Stateless 서버:** 세션 상태는 Redis에 저장
- 서버 수평 확장 가능

### 9.3 비동기 처리

- **작업 큐 사용** (예: Bull, Celery)
- LLM 호출 등 무거운 작업은 큐를 통해 처리

### 9.4 LLM 스트리밍

#### 9.4.1 적용 범위

| 노드 | 스트리밍 |
|------|----------|
| RAG 답변 생성 | ✅ |
| LLM 기반 메시지 출력 | ✅ |
| 의도 분류 (짧은 응답) | ❌ |

#### 9.4.2 대기 UX

- 오래 걸릴 때 대기 메시지 표시

#### 9.4.3 노드 상태 관리

- 스트리밍 중: "실행 중" 상태
- **스트리밍 완료 후에만** 결과 저장 → 상태 "완료"
- 무결성 및 재시도 대비

#### 9.4.4 스트리밍 중 새 입력 처리

- **스트리밍 완료까지 대기** 후 처리
- 새 입력은 큐에 대기

이유:

- 노드 상태 명확성 유지
- 체크포인트 무결성 보장
- 구현 단순화

### 9.5 실서비스 전환 시 고려사항

| 항목 | MVP | 실서비스 |
|------|-----|---------|
| Redis 구성 | 단일 인스턴스 | Sentinel/Cluster |
| 세션 동기화 | 종료 시만 PostgreSQL | Write-through |
| 장애 대응 | 유실 감수 | 체크포인트 복구 |

---

## 부록: 노드 목록 요약

| 분류 | 노드 | Blocking | 핵심 특징 |
|------|------|----------|----------|
| 메시지 | 메시지 출력 | 옵션 (기본 ON) | 다중 메시지, 리치 메시지, 버튼 payload |
| AI | RAG 검색 | ❌ | 문서셋(폴더) 선택, 벡터 유사도 confidence |
| AI | 의도 분류 | ❌ | 분기 역할 겸함, 의도별 핸들러 |
| 로직 | 조건 분기 | ❌ | 변수 기반 조건 분기 |
| 로직 | Parallel | ❌ | 명시적 핸들러 수 지정 |
| 로직 | Join | ❌ | Parallel과 짝, Non-blocking만 허용 |
| 통합 | API 커넥터 | ❌ | OAuth 자동 갱신, JSONPath 매핑 |
| 통합 | 커스텀 코드 | ❌ | JS 샌드박스, 10초/64MB 제한 |
| HITL | 에스컬레이션 | ✅ | 상담원 이관, 요약 전달, 복귀 없음 |
| HITL | 승인 | ✅ | 비동기 승인, AI 요약 + 커스텀 메시지 |
| 시스템 | 시작 | - | 채팅/이메일/API 트리거 |
| 시스템 | 종료 | - | 세션 보관, 만족도 조사 연계 가능 |
| 시스템 | 에러 폴백 | - | 노드별 또는 전역 |

---

**문서 끝**
